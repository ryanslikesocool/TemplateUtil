using System;
using Unity.Collections;
using Unity.Entities;
using Unity.Mathematics;

#ROOTNAMESPACEBEGIN#
    [Serializable, InternalBufferCapacity(x)]
    public struct #SCRIPTNAME# : IBufferElementData
    {
        public T Value;

        public static implicit operator T(#SCRIPTNAME# b) { return b.Value; }
        public static implicit operator #SCRIPTNAME#(T b) { return new T { Value = b }; }
    }
#ROOTNAMESPACEEND#